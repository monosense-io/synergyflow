apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gateway-dev
  namespace: flux-system
  labels:
    app.kubernetes.io/part-of: synergyflow
spec:
  interval: 1m0s
  path: ./infra/gateway/overlays/dev
  prune: true
  wait: true
  validation: client
  targetNamespace: edge
  sourceRef:
    kind: GitRepository
    name: synergyflow

